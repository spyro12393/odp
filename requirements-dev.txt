# Dev/test dependencies. Mirrors [project.optional-dependencies].dev in pyproject.toml.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
