# Runtime dependencies for os_data_platform.
# Source of truth is pyproject.toml; this file mirrors [project].dependencies
# so plain `pip install -r requirements.txt` works for environments without uv.
#
# For dev/test deps, see requirements-dev.txt.

pydantic>=2.5
pyyaml>=6.0
